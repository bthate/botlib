#!/usr/bin/python3
# BOTLIB - framework to program bots
#
#

import ol
import os

def main():
    k = ol.krn.boot("bot", os.path.expanduser("~/.bot"))
    k.walk("bmod")
    k.cmd(k.cfg.otxt)

ol.trm.execute(main)
os._exit(0)
