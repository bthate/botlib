#!/usr/bin/python3
# BOTLIB - the bot library !
#
#

import os, sys ; sys.path.insert(0, os.getcwd())
from ol.spc import boot, cmd, execute, sys

def main():
    k = boot("kcmd", os.path.expanduser("~/.bot"))
    cmd(k.cfg.txt, k.cfg.wd)
   

execute(main)

