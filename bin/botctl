#!/usr/bin/python3 -u
# BOTLIB - Framework to program bots.
#
# bin/botctl (bot control)

import lo
import os
import sys

from bot import k
from lo inport execute

def main():
    lo.workdir = "/var/lib/botd"
    k.walk("bot")
    k.cmd(" ".join(sys.argv[1:]))

execute(main)
os._exit(0)
