#!/usr/bin/python3 -u
# BOTLIB - Framework to program bots.
#
# bin/botctl (bot control)

import lo
import os
import sys

from bot import k

def main():
    lo.workdir = "/var/lib/botd"
    k.walk("bot")
    print(k)
    k.cmd(" ".join(sys.argv[1:]))
    
lo.shl.execute(main)
os._exit(0)
