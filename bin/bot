#!/usr/bin/python3
# BOTLIB - the bot library !
#
#

from ol.spc import Console, boot, execute, os

def main():
    k = boot("bot", os.path.expanduser("~/.bot"))
    k.start()
    k.init(k.cfg.mods)
    c = Console()
    c.start()
    k.wait()

execute(main)
os._exit(0)
