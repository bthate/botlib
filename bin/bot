#!/usr/bin/python3 -u
# BOTLIB - the bot library !
#
#

__version__ = 86

from bot.utl.shl import execute, parse_cli
from bot.csl import Console
from bot.irc import IRC
from bot.krn import k

def main():
    cfg = parse_cli("bot")
    k.start(cfg)
    i = IRC()
    i.start(cfg)
    c = Console()
    c.start(cfg)
    k.wait()

execute(main)
