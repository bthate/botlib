# cfg.txt

"""
    >>> import bot.obj
    >>> cfg = bot.obj.Cfg()
    >>> cfg.server = "localhost"
    >>> cfg.channel = "#botd"
    >>> cfg.nick = "botddd"
    >>> print(cfg)
    {
        "channel": "#botd",
        "nick": "botddd",
        "server": "localhost"
    }

"""

